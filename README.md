# Console Assistant - AI Чат-бот с поддержкой OpenAI и Claude

Консольный и Telegram бот-ассистент с поддержкой множества AI моделей, включая GPT-3.5, GPT-4o, GPT-5 Pro, O1, O3 и Claude 4.5 Sonnet. Бот поддерживает диалог с сохранением контекста и автоматическим сохранением истории.

## 🚀 Возможности

- **Поддержка множества моделей AI:**
  - OpenAI: GPT-3.5 Turbo, GPT-4o, GPT-5 Pro, O1, O3
  - Anthropic: Claude 4.5 Sonnet (с reasoning метриками)

- **Режим диалога:**
  - Сохранение контекста разговора
  - Автоматическое сохранение истории в JSON и TXT файлы
  - Загрузка истории при перезапуске

- **Telegram бот:**
  - Интерактивное меню с кнопками
  - Выбор модели через меню
  - Автоматическое сохранение истории для каждого пользователя
  - Скачивание истории диалога
  - Reasoning метрики для Claude моделей

- **Консольный режим:**
  - Интерактивный диалог в терминале
  - Выбор модели при запуске
  - Экспорт истории

- **Логирование:**
  - Автоматическое логирование всех ошибок
  - Логирование параметров запросов к API
  - Логирование событий приложения
  - Ротация логов (автоматическая очистка старых файлов)

## 📋 Требования

- Python 3.10 или выше
- Виртуальное окружение (рекомендуется)
- API ключ от ProxyAPI (для доступа к OpenAI и Anthropic через прокси)

## 🔧 Установка

### 1. Клонирование репозитория

```bash
cd "C:\Users\user\Documents\ZEROCODING\CODER PROFESSION\MODUL 4\VPd06\Console Assistant"
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
```

### 3. Активация виртуального окружения

**Windows PowerShell:**
```powershell
.\venv\Scripts\Activate.ps1
```

**Windows CMD:**
```cmd
venv\Scripts\activate.bat
```

### 4. Установка зависимостей

```bash
pip install -r requrements.txt
```

## ⚙️ Настройка

### Создание файла .env

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
ProxyAPI_KEY=ваш_ключ_от_proxyapi
TELEGRAM_BOT_KEY=ваш_токен_telegram_бота
```

**Важно:** Файл `.env` уже должен существовать (находится в .gitignore). Просто добавьте в него необходимые ключи.

### Получение API ключей

1. **ProxyAPI_KEY:**
   - Зарегистрируйтесь на [proxyapi.ru](https://proxyapi.ru)
   - Получите API ключ в личном кабинете

2. **TELEGRAM_BOT_KEY:**
   - Создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
   - Скопируйте токен бота

## 📖 Использование

### Консольный режим

Запустите интерактивный диалог:

```bash
python ai_assistant.py
```

Программа предложит:
1. Выбрать модель (по умолчанию: claude-sonnet-4-5-20250929)
2. Настроить reasoning параметры (для self-reasoning моделей)
3. Начать диалог

**Команды в консольном режиме:**
- Просто вводите сообщения для диалога
- `exit`, `quit`, `выход`, `стоп` - выход из программы
- `export`, `save` - экспорт истории в текстовый файл

### Telegram бот

Запустите бота:

```bash
python telegram_bot.py
```

**Команды бота:**
- `/start` - начать работу (открывает меню)
- `/menu` - открыть главное меню
- `/model` - выбрать модель
- `/clear` - очистить историю диалога
- `/export` - скачать историю диалога
- `/exit` - информация о выходе из бота
- `/help` - показать справку

**Текстовые команды:**
- Напишите `exit`, `меню` или `menu` - откроется главное меню

**Боковая панель Telegram:**
- Нажмите на иконку меню (☰) рядом с полем ввода
- Все команды доступны с описаниями

## 📁 Структура проекта

```
Console Assistant/
├── ai_assistant.py           # Основной модуль с ChatAssistant
├── telegram_bot.py           # Telegram бот
├── config.py                 # Конфигурация проекта
├── context_manager.py        # Управление контекстом пользователей
├── logger.py                 # Модуль логирования
├── requrements.txt           # Зависимости проекта
├── README.md                 # Документация
├── .env                      # Переменные окружения (не в git)
├── logs/                     # Папка с логами
│   ├── errors.log            # Логи ошибок
│   ├── requests.log          # Логи запросов к API
│   └── app.log               # Общие логи приложения
├── chat_history_{user_id}.json  # История для каждого пользователя Telegram
├── chat_history_{user_id}.txt   # История для каждого пользователя (текст)
└── venv/                     # Виртуальное окружение
```

## 🔑 API Endpoints

Проект использует прокси-сервер для доступа к API:

- **OpenAI API:** `https://api.proxyapi.ru/openai/v1`
- **Anthropic API:** `https://api.proxyapi.ru/anthropic`

## 📝 Логирование

Проект включает систему логирования для отслеживания ошибок и параметров запросов:

- **errors.log** - все ошибки с полным стеком вызовов
- **requests.log** - параметры запросов к API (модель, токены, время ответа)
- **app.log** - общие события приложения (запуск бота, смена модели, и т.д.)

Логи сохраняются в папке `logs/` с автоматической ротацией:
- Максимальный размер файла: 10 МБ
- Количество резервных файлов: 5
- Старые логи автоматически архивируются

**Важно:** Логи не содержат чувствительных данных (API ключи скрыты).

## 💾 Сохранение истории

История диалога автоматически сохраняется:

- **JSON формат:** `chat_history.json` (или `chat_history_{user_id}.json` для Telegram)
  - Сохраняется после каждого сообщения
  - Загружается при следующем запуске
  - Содержит полную историю с метаданными

- **TXT формат:** `chat_history.txt` (или `chat_history_{user_id}.txt` для Telegram)
  - Создается автоматически после каждого сообщения
  - Удобен для чтения
  - Можно скачать через Telegram бота

## 📊 Reasoning метрики

Для Claude моделей отображаются reasoning метрики:
- Входные токены
- Выходные токены
- Токены создания кэша (если доступно)
- Токены чтения кэша (если доступно)
- Причина остановки

Для OpenAI моделей отображаются:
- Промпт токены
- Токены ответа
- Всего токенов

## 🛠️ Разработка

### Основные классы и функции

**ChatAssistant** (`ai_assistant.py`):
- `__init__()` - инициализация ассистента
- `get_response()` - получение ответа от AI
- `clear_history()` - очистка истории
- `save_history()` - сохранение истории в JSON
- `load_history()` - загрузка истории из JSON
- `export_history_to_text()` - экспорт в текстовый файл

**Telegram бот** (`telegram_bot.py`):
- Отдельная история для каждого пользователя
- Интерактивное меню с кнопками
- Автоматическое сохранение истории

## ⚠️ Важные замечания

1. **Прокси-сервер не поддерживает reasoning параметры:**
   - Параметр `reasoning_effort` не передается для OpenAI моделей
   - Модели работают, но без специальных reasoning настроек

2. **Один экземпляр бота:**
   - Запускайте только один экземпляр Telegram бота одновременно
   - При конфликте остановите все процессы Python и перезапустите

3. **Файл .env:**
   - Не коммитьте `.env` файл в git
   - Храните ключи в безопасности

## 🐛 Решение проблем

### Ошибка "TELEGRAM_BOT_KEY не найден"
- Убедитесь, что файл `.env` существует
- Проверьте, что ключ указан правильно: `TELEGRAM_BOT_KEY=ваш_токен`

### Ошибка "Conflict: terminated by other getUpdates request"
- Остановите все запущенные экземпляры бота
- Используйте: `taskkill /F /IM python.exe` (Windows)

### Ошибка кодировки в Windows
- Код автоматически настраивает UTF-8 для Windows консоли
- Если проблемы остаются, проверьте настройки консоли

### Меню не открывается
- Перезапустите бота после изменений
- Проверьте, что обработчик `menu_command` зарегистрирован
- Используйте `/start` для открытия меню

## 📝 Лицензия

Проект создан в образовательных целях.

## 👤 Автор

Создано для курса "ZEROCODING CODER PROFESSION MODUL 4"

## 🔗 Полезные ссылки

- [ProxyAPI Documentation](https://proxyapi.ru/docs)
- [OpenAI API Documentation](https://platform.openai.com/docs)
- [Anthropic API Documentation](https://docs.anthropic.com)
- [Python Telegram Bot](https://python-telegram-bot.org)

